$kind: http-request
description: <p>Retrieves a specific cash balance transaction, which updated the
  customer’s <a href="/docs/payments/customer-balance">cash balance</a>.</p>
url: "{{baseUrl}}/v1/customers/:customer/cash_balance_transactions/:transaction"
method: GET
headers:
  Content-Type: application/x-www-form-urlencoded
queryParams:
  - key: expand[0]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
  - key: expand[1]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
pathVariables:
  - key: customer
    value: <string>
    description: "(Required) "
  - key: transaction
    value: <string>
    description: "(Required) "
order: 2000
