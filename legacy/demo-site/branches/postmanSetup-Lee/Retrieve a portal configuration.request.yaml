$kind: http-request
description: <p>Retrieves a configuration that describes the functionality of
  the customer portal.</p>
url: "{{baseUrl}}/v1/billing_portal/configurations/:configuration"
method: GET
headers:
  Content-Type: application/x-www-form-urlencoded
queryParams:
  - key: expand[0]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
  - key: expand[1]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
pathVariables:
  - key: configuration
    value: <string>
    description: "(Required) "
order: 3000
