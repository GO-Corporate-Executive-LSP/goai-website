$kind: http-request
description: <p>Retrieves a product_feature, which represents a feature
  attachment to a product</p>
url: "{{baseUrl}}/v1/products/:product/features/:id"
method: GET
headers:
  Content-Type: application/x-www-form-urlencoded
queryParams:
  - key: expand[0]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
  - key: expand[1]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
pathVariables:
  - key: id
    value: <string>
    description: (Required) The ID of the product_feature.
  - key: product
    value: <string>
    description: (Required) The ID of the product.
order: 4000
