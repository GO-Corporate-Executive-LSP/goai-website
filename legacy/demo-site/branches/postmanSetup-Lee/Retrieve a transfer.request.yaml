$kind: http-request
description: <p>Retrieves the details of an existing transfer. Supply the unique
  transfer ID from either a transfer creation request or the transfer list, and
  Stripe will return the corresponding transfer information.</p>
url: "{{baseUrl}}/v1/transfers/:transfer"
method: GET
headers:
  Content-Type: application/x-www-form-urlencoded
queryParams:
  - key: expand[0]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
  - key: expand[1]
    value: <string>
    disabled: true
    description: Specifies which fields in the response should be expanded.
pathVariables:
  - key: transfer
    value: <string>
    description: "(Required) "
order: 3000
