$kind: http-request
description: Insert rideID into endpoint address
url: "{{url}}/v1/venues?lat={{origin_lat}}&lng={{origin_lng}}&type={{venue_type}}"
method: GET
queryParams:
  lat: "{{origin_lat}}"
  lng: "{{origin_lng}}"
  type: "{{venue_type}}"
order: 4000
