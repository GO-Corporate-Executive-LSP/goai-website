$kind: http-request
description: <p>Creates a payment link.</p>
url: "{{baseUrl}}/v1/payment_links"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
body:
  type: urlencoded
  content:
    - key: line_items[0][price]
      value: <string>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[0][quantity]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[0][adjustable_quantity][enabled]
      value: <boolean>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[0][adjustable_quantity][maximum]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[0][adjustable_quantity][minimum]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[1][price]
      value: <string>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[1][quantity]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[1][adjustable_quantity][enabled]
      value: <boolean>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[1][adjustable_quantity][maximum]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: line_items[1][adjustable_quantity][minimum]
      value: <integer>
      description: (Required) The line items representing what is being sold. Each
        line item represents an item being sold. Up to 20 line items are
        supported.
    - key: after_completion[type]
      value: <string>
      disabled: true
      description: Behavior after the purchase is complete.
    - key: after_completion[hosted_confirmation][custom_message]
      value: <string>
      disabled: true
      description: Behavior after the purchase is complete.
    - key: after_completion[redirect][url]
      value: <string>
      disabled: true
      description: Behavior after the purchase is complete.
    - key: allow_promotion_codes
      value: <boolean>
      disabled: true
      description: Enables user redeemable promotion codes.
    - key: application_fee_amount
      value: <integer>
      disabled: true
      description: The amount of the application fee (if any) that will be requested
        to be applied to the payment and transferred to the application owner's
        Stripe account. Can only be applied when there are no line items with
        recurring prices.
    - key: application_fee_percent
      value: <number>
      disabled: true
      description: A non-negative decimal between 0 and 100, with at most two decimal
        places. This represents the percentage of the subscription invoice total
        that will be transferred to the application owner's Stripe account.
        There must be at least 1 line item with a recurring price to use this
        field.
    - key: automatic_tax[enabled]
      value: <boolean>
      disabled: true
      description: Configuration for automatic tax collection.
    - key: automatic_tax[liability][type]
      value: <string>
      disabled: true
      description: Configuration for automatic tax collection.
    - key: automatic_tax[liability][account]
      value: <string>
      disabled: true
      description: Configuration for automatic tax collection.
    - key: billing_address_collection
      value: <string>
      disabled: true
      description: Configuration for collecting the customer's billing address.
        Defaults to `auto`.
    - key: consent_collection[payment_method_reuse_agreement][position]
      value: <string>
      disabled: true
      description: Configure fields to gather active consent from customers.
    - key: consent_collection[promotions]
      value: <string>
      disabled: true
      description: Configure fields to gather active consent from customers.
    - key: consent_collection[terms_of_service]
      value: <string>
      disabled: true
      description: Configure fields to gather active consent from customers.
    - key: currency
      value: <string>
      disabled: true
      description: Three-letter [ISO currency
        code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        Must be a [supported currency](https://stripe.com/docs/currencies) and
        supported by each line item's price.
    - key: custom_fields[0][key]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][label][custom]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][label][type]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][type]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][dropdown][options][0][label]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][dropdown][options][0][value]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][dropdown][options][1][label]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][dropdown][options][1][value]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][numeric][maximum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][numeric][minimum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][optional]
      value: <boolean>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][text][maximum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[0][text][minimum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][key]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][label][custom]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][label][type]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][type]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][dropdown][options][0][label]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][dropdown][options][0][value]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][dropdown][options][1][label]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][dropdown][options][1][value]
      value: <string>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][numeric][maximum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][numeric][minimum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][optional]
      value: <boolean>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][text][maximum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_fields[1][text][minimum_length]
      value: <integer>
      disabled: true
      description: Collect additional information from your customer using custom
        fields. Up to 3 fields are supported.
    - key: custom_text[after_submit][message]
      value: <string>
      disabled: true
      description: Display additional text for your customers using custom text.
    - key: custom_text[shipping_address][message]
      value: <string>
      disabled: true
      description: Display additional text for your customers using custom text.
    - key: custom_text[submit][message]
      value: <string>
      disabled: true
      description: Display additional text for your customers using custom text.
    - key: custom_text[terms_of_service_acceptance][message]
      value: <string>
      disabled: true
      description: Display additional text for your customers using custom text.
    - key: customer_creation
      value: <string>
      disabled: true
      description: Configures whether [checkout
        sessions](https://stripe.com/docs/api/checkout/sessions) created by this
        payment link create a [Customer](https://stripe.com/docs/api/customers).
    - key: expand[0]
      value: <string>
      disabled: true
      description: Specifies which fields in the response should be expanded.
    - key: expand[1]
      value: <string>
      disabled: true
      description: Specifies which fields in the response should be expanded.
    - key: inactive_message
      value: <string>
      disabled: true
      description: The custom message to be displayed to a customer when a payment
        link is no longer active.
    - key: invoice_creation[enabled]
      value: <boolean>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][account_tax_ids][0]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][account_tax_ids][1]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][custom_fields][0][name]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][custom_fields][0][value]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][custom_fields][1][name]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][custom_fields][1][value]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][description]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][footer]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][issuer][type]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][issuer][account]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][metadata]
      value: <object>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: invoice_creation[invoice_data][rendering_options][amount_tax_display]
      value: <string>
      disabled: true
      description: Generate a post-purchase Invoice for one-time payments.
    - key: on_behalf_of
      value: <string>
      disabled: true
      description: The account on behalf of which to charge.
    - key: payment_intent_data[capture_method]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[description]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[metadata]
      value: <object>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[setup_future_usage]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[statement_descriptor]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[statement_descriptor_suffix]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_intent_data[transfer_group]
      value: <string>
      disabled: true
      description: A subset of parameters to be passed to PaymentIntent creation for
        Checkout Sessions in `payment` mode.
    - key: payment_method_collection
      value: <string>
      disabled: true
      description: >-
        Specify whether Checkout should collect a payment method. When set to
        `if_required`, Checkout will not collect a payment method when the total
        due for the session is 0.This may occur if the Checkout Session includes
        a free trial or a discount.


        Can only be set in `subscription` mode. Defaults to `always`.


        If you'd like information on how to collect a payment method outside of
        Checkout, read the guide on [configuring subscriptions with a free
        trial](https://stripe.com/docs/payments/checkout/free-trials).
    - key: payment_method_types[0]
      value: <string>
      disabled: true
      description: The list of payment method types that customers can use. If no
        value is passed, Stripe will dynamically show relevant payment methods
        from your [payment method
        settings](https://dashboard.stripe.com/settings/payment_methods) (20+
        payment methods
        [supported](https://stripe.com/docs/payments/payment-methods/integration-options#payment-method-product-support)).
    - key: payment_method_types[1]
      value: <string>
      disabled: true
      description: The list of payment method types that customers can use. If no
        value is passed, Stripe will dynamically show relevant payment methods
        from your [payment method
        settings](https://dashboard.stripe.com/settings/payment_methods) (20+
        payment methods
        [supported](https://stripe.com/docs/payments/payment-methods/integration-options#payment-method-product-support)).
    - key: phone_number_collection[enabled]
      value: <boolean>
      disabled: true
      description: >-
        Controls phone number collection settings during checkout.


        We recommend that you review your privacy policy and check with your
        legal contacts.
    - key: restrictions[completed_sessions][limit]
      value: <integer>
      disabled: true
      description: Settings that restrict the usage of a payment link.
    - key: shipping_address_collection[allowed_countries][0]
      value: <string>
      disabled: true
      description: Configuration for collecting the customer's shipping address.
    - key: shipping_address_collection[allowed_countries][1]
      value: <string>
      disabled: true
      description: Configuration for collecting the customer's shipping address.
    - key: shipping_options[0][shipping_rate]
      value: <string>
      disabled: true
      description: The shipping rate options to apply to [checkout
        sessions](https://stripe.com/docs/api/checkout/sessions) created by this
        payment link.
    - key: shipping_options[1][shipping_rate]
      value: <string>
      disabled: true
      description: The shipping rate options to apply to [checkout
        sessions](https://stripe.com/docs/api/checkout/sessions) created by this
        payment link.
    - key: submit_type
      value: <string>
      disabled: true
      description: "Describes the type of transaction being performed in order to
        customize relevant text on the page, such as the submit button. Changing
        this value will also affect the hostname in the
        [url](https://stripe.com/docs/api/payment_links/payment_links/object#ur\
        l) property (example: `donate.stripe.com`)."
    - key: subscription_data[description]
      value: <string>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: subscription_data[invoice_settings][issuer][type]
      value: <string>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: subscription_data[invoice_settings][issuer][account]
      value: <string>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: subscription_data[metadata]
      value: <object>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: subscription_data[trial_period_days]
      value: <integer>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: subscription_data[trial_settings][end_behavior][missing_payment_method]
      value: <string>
      disabled: true
      description: When creating a subscription, the specified configuration data will
        be used. There must be at least one line item with a recurring price to
        use `subscription_data`.
    - key: tax_id_collection[enabled]
      value: <boolean>
      disabled: true
      description: Controls tax ID collection during checkout.
    - key: transfer_data[destination]
      value: <string>
      disabled: true
      description: The account (if any) the payments will be attributed to for tax
        reporting, and where funds from each payment will be transferred to.
    - key: transfer_data[amount]
      value: <integer>
      disabled: true
      description: The account (if any) the payments will be attributed to for tax
        reporting, and where funds from each payment will be transferred to.
order: 2000
